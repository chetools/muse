# GitHub Pages branch

Auto-generated WebAssembly export of the tanks-in-series NumPy/SciPy marimo
notebook (`tube/marimo/tanks_in_series_numpy.py` on the `tube` branch).

Do not edit by hand — regenerate with:

    marimo export html-wasm tube/marimo/tanks_in_series_numpy.py \
        -o <output-dir> --mode run -f

then set `<title>` to `Tanks-in-Series RTD Explorer` in `index.html`,
and force-push this branch.
